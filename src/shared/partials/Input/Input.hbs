<input
       class="input {{#if className}} {{ className }}{{/if}}"
       id="{{id}}"
       type="{{ type }}"
       title="{{ title }}"
       name="{{ name }}"
       value="{{value}}"
       placeholder="{{placeholder}}"
       {{#if readonly}}readonly{{/if}}>
