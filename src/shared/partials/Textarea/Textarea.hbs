<textarea
          class="textarea {{#if className}} {{ className }}{{/if}}"
          id="{{id}}"
          title="{{ title }}"
          name="{{ name }}"
          placeholder="{{placeholder}}">{{value}}</textarea>